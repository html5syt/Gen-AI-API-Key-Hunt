services:
  app:
    image: ${GHCR_IMAGE:?set GHCR_IMAGE to ghcr.io/<owner>/<repo>:latest}
    pull_policy: always
    ports:
      - "8080:8080"
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - ./data:/app/data
    restart: unless-stopped
